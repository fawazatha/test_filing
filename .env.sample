# =========================
# Core App
# =========================
# Node/Python will usually respect this for logs; adjust as needed
APP_ENV=development                 # development | staging | production
LOG_LEVEL=INFO                      # DEBUG | INFO | WARNING | ERROR
APP_TZ=Asia/Jakarta                 # IANA timezone

# Optional outbound proxy (HTTP/HTTPS). Leave empty to disable.
PROXY=                               # e.g. http://user:pass@proxy.host:8080

# =========================
# Supabase (Required)
# =========================
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_KEY=                        # anon or service role key 
SUPABASE_SCHEMA=                     # optional, default: public
SUPABASE_TABLE_FILINGS=              # table names used by your pipeline
SUPABASE_TABLE_NEWS=

# =========================
# LLM Providers (Optional; pick one)
# =========================
# --- Google Gemini ---
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-pro-latest

# --- Groq ---
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-70b-versatile

# --- OpenAI ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Which LLM to use by default if your code supports a switch
LLM_PROVIDER=groq                    # openai | groq | gemini

# =========================
# Email (choose ONE provider or keep both and switch via EMAIL_PROVIDER)
# =========================
EMAIL_PROVIDER=ses                   # ses | sendgrid

# --- AWS SES (SMTP-less via SDK) ---
AWS_REGION=ap-southeast-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
SES_FROM_EMAIL=alerts@example.com
TEST_TO_EMAIL=you@example.com        # for dry-runs/tests
ALERT_TO_EMAIL=ops@example.com       # comma-separated for multiple recipients

# --- SendGrid (alternative to SES) ---
SENDGRID_API_KEY=
SENDGRID_FROM=alerts@example.com
SENDGRID_TO=ops@example.com          # comma-separated for multiple recipients

# =========================
# Company Map / Cache (Optional)
# =========================
# If your pipeline builds a company map/cached metadata
COMPANY_TABLE_NAME=
COMPANY_UPDATED_COL=updated_at
COMPANY_SCHEMA=public

# Cache behavior
COMPANY_MAP_TTL_HOURS=24             # integer hours; 0 to always refresh
COMPANY_MAP_ALLOW_OFFLINE=false      # true/false — allow cached reads if upstream is down
COMPANY_MAP_FORCE_REFRESH=false      # true/false — ignore cache once on next run

# =========================
# Safety / Defaults Notes
# =========================
# - Booleans should be: true/false (lowercase) unless your config parser says otherwise.
# - Lists should be comma-separated with no spaces (or handle trimming in code).
# - Never commit real secrets; keep this file as a template only.
